export interface iClient {
    id: string
	created_at: Date
	name: string
	email: string
	phone: string
}

export interface iContact {
    id: string
    created_at: string
    name: string
    email: string
    phone: string
}

